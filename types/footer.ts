import { Sys } from 'types/sys';

export interface FooterTypes {
  fields: {
    brandInfos: string;
    facebookLink: string;
    twitterLink: string;
    instagramLink: string;
  };
  metadata: {};
  sys: Sys;
}
